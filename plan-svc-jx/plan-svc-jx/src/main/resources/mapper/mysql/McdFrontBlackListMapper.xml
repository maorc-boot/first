<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asiainfo.biapp.pec.plan.jx.hmh5.dao.McdFrontBlacklistCustDao">


  <!--excel导入批量-->
  <insert id="batchSaveBlacklist" parameterType="list">

    INSERT INTO mcd_front_blacklist_cust (PRODUCT_NO, CREATE_BY, DATA_STATE,
    EFFECT_END_TIME, REMARKS, JOIN_TYPE, CITY_ID)

    VALUES
    <foreach collection="collection" item="item" index="index" separator=",">
      <trim prefix="(" suffix=")" suffixOverrides=",">
        #{item.procuctNo,jdbcType=VARCHAR},
        #{item.createBy,jdbcType=VARCHAR},
        #{item.dataState,jdbcType=INTEGER},
        #{item.effectEndTime,jdbcType=TIMESTAMP},
        #{item.remarks,jdbcType=VARCHAR},
        #{item.joinType,jdbcType=INTEGER},
        #{item.cityId,jdbcType=VARCHAR}

      </trim>
    </foreach>

  </insert>


</mapper>
